import 'package:flutter/material.dart';

class GameVerse extends StatelessWidget {
  const GameVerse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('character\ncollections of games\ncompany\nfranchise\ngameEngines\nlanguage support\nplatform'),
      ],
    );
  }
}

